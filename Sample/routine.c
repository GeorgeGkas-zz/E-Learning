 if (ProfitA == ProfitB == ProfitC) {
            bestMin = ProfitA; //the person don't matter
            printf("\n\tThe optimal best solution is %d\n", bestMin);
            return 0;
        }

//is the following work as the above?

if (ProfitA == ProfitC ) {
            if (ProfitB = ProfitA) {
            bestMin = ProfitA;
            printf("\n\tThe optimal best solution is %d\n", bestMin);
            return 0;
            }
        }
