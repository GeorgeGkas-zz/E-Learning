if (ProfitA == ProfitC ) {
            if (ProfitB == ProfitA) {
            bestMin = ProfitA;
            printf("\n\tThe optimal best solution is %d\n", bestMin);
            return 0;
            }
        }
